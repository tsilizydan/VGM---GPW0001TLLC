# Deny all direct HTTP access to the storage directory.
# Cache files, logs, and uploaded temp files must never be
# accessible from the browser.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
